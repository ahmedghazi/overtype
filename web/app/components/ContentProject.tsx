import React from "react";
import { Project } from "../types/schema";

type Props = {
  input: Project;
};

const ContentProject = (props: Props) => {
  return <div>ContentProject</div>;
};

export default ContentProject;
